import React from 'react';

interface FormBoxProps {
  name: string;
  handleChange: () => void;
  isAlert:boolean; 
}

const FormBox = () => {
  return <div>FormBox </div>;
};

export default FormBox;
